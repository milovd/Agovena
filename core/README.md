# core/

Agovena Core application logic lives in the Laravel app (`app/`, `routes/`, etc.) during Phase 1.

This directory remains a **documentation boundary** for the conceptual Core layer (catalog, orders, payments abstraction, platform runtime). Extract Composer packages only when Phase implementation requires them — do not add empty package trees here yet.
